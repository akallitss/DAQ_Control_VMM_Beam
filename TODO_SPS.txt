================================================================================
 TODO-SPS CHECKLIST — things to fill in on the VMM DAQ computer at the beam
================================================================================
Every item below is marked TODO-SPS in the code (grep -rn TODO-SPS).
Work through them top to bottom on the DAQ machine; when done, switch
SITE = 'sps' in run_config_beam.py and run:
    .venv/bin/python run_config_beam.py     # prints a summary — read it!

--------------------------------------------------------------------------------
 1. base_data_dir                                    run_config_beam.py (SITES)
--------------------------------------------------------------------------------
 What : directory on the DAQ machine's DATA disk where runs/ analysis/ etc.
        are created. Keep the trailing slash.
 How  : find the big data partition:
            df -h                      # look for the large, mostly-empty disk
            lsblk -f                   # partition layout
        Create the tree and make sure your user can write:
            mkdir -p /local/p2/p2data/sps_vmm_2026 && touch /local/p2/p2data/sps_vmm_2026/.w
        Also copy config/detectors/ contents there (define_detectors etc. are
        read from <base_data_dir>/config/detectors/).

--------------------------------------------------------------------------------
 2. daq_host                                         run_config_beam.py (SITES)
--------------------------------------------------------------------------------
 What : IP where daq_control/flask reach the hv/lv/daq servers.
 How  : everything runs on the SAME machine -> leave 127.0.0.1. Done.
        (Only change it if services are ever split across machines; then use
        `hostname -I` on the server machine.)
        Separately: to open the GUI from the control room you need the DAQ
        machine's LAN IP — `hostname -I` or `ip -br addr` — and port 5002
        reachable (check any local firewall: `sudo ufw status` /
        `sudo firewall-cmd --list-ports`).

--------------------------------------------------------------------------------
 3. hv_ip  (CAEN mainframe)                          run_config_beam.py (SITES)
--------------------------------------------------------------------------------
 What : IP of the CAEN HV crate on the network the DAQ machine sees.
 How  , in order of reliability:
        a) Read it on the crate itself: front-panel display/menu shows the
           LAN settings (or plug in the local keypad/screen).
        b) If it was configured before, it is often written on a label/tape
           on the crate, or known by whoever set up the crate (Dream DAQ
           uses the same crate family — check DAQ_Control_Dream_Beam's
           run_config_beam.py for the value already in use at SPS).
        c) Scan the crate subnet from the DAQ machine:
               sudo arp-scan --interface=<nic> 192.168.10.0/24
               # or:  nmap -sn 192.168.10.0/24
           CAEN mainframes show a CAEN OUI in the MAC vendor column.
        Test:  ping <hv_ip>   and log in once with the CAEN GECO/web tool.

--------------------------------------------------------------------------------
 4. hv_n_cards  +  P2_HV channel map                 run_config_beam.py
--------------------------------------------------------------------------------
 What : number of HV cards in the SPS crate, and (card, channel) for the P2
        mesh and drift lines.
 How  : count the cards physically / check the crate inventory, then follow
        the SHV cables: which card slot + channel number the mesh and drift
        of P2 are plugged into. Update:
            hv_n_cards            (SITES['sps'])
            P2_HV = {'mesh': (card, ch), 'drift': (card, ch)}
        The GUI HV plot labels (P2_1_Mesh / P2_1_Drift) follow from this map.
        Verify by ramping a few volts from the CAEN tool and watching which
        channel moves.

--------------------------------------------------------------------------------
 5. lv_units  (Aim-TTi PSU IPs)                      run_config_beam.py (SITES)
--------------------------------------------------------------------------------
 What : LAN IPs of the two TTi bench supplies (tti1, tti2).
 How  : a) On each unit's front panel: Menu/Utilities -> Interface -> LAN
           shows the current IP (they may be DHCP — consider setting a
           static IP or a DHCP reservation so it survives power cycles).
        b) LXI discovery from the DAQ machine:
               lxi discover            # apt install lxi-tools
        c) Subnet scan as in item 3 (vendor "Aim-TTi"/"TTi").
        Test SCPI directly (port 9221 is fixed on these units):
            nc -zv <ip> 9221
            printf '*IDN?\n' | nc -q1 <ip> 9221      # should return the IDN string
        Note which physical rail is on which unit+channel and set
        lv_info['units'][name]['channels'] if not all 3 channels are used.

--------------------------------------------------------------------------------
 6. interfaces  (capture NICs + ALINX config)        run_config_beam.py (SITES)
--------------------------------------------------------------------------------
 What : Linux name of each NIC that receives VMM UDP, and the absolute path
        of the alinx-sc JSON config on the DAQ machine.
 How  : list NICs and see which has a link up on the readout fibre/cable:
            ip -br link                 # UP/DOWN per interface
            ethtool <iface>             # link detected: yes
        Confirm data actually arrives on it (with the front-end sending):
            sudo tcpdump -i <iface> -nn udp -c 5
        The UDP *source* IP printed there is the FEC/ALINX — the QA
        auto-detects it, nothing to configure, but it is a good check.
        ALINX config: locate the file used to arm the firmware:
            locate config_alinx_noThresholds.json   # or: find / -name 'config_alinx*' 2>/dev/null
        and test the slow control end to end:
            alinx-sc --config-file <path> --read-link-status
        SRS chain: if also capturing SRS, uncomment the 'srs' entry and set
        its iface the same way (slow_control: 'none').

--------------------------------------------------------------------------------
 7. trigger description                              run_config_beam.py
--------------------------------------------------------------------------------
 What : self.trigger — free-text description of the actual trigger formation
        at SPS (goes into run_config.json for bookkeeping/analysis).
 How  : write down the real setup once cabled, e.g. "scintillator coincidence
        S1&S2 -> TCM ext trigger" or "self-triggered". If running with an
        external trigger, also switch the QA to marker parsing:
            qa_config.py: 'data_format': 'TRG'   (then rerun it to regenerate)

--------------------------------------------------------------------------------
 8. det_center_coords                                run_config_beam.py
--------------------------------------------------------------------------------
 What : x/y/z of the P2 detector centre in beam-line coordinates.
 How  : from the survey after installation (ask the survey team / take the
        table position + detector offset). Informational, but saved with
        every run — fill before production data.

--------------------------------------------------------------------------------
 9. vmm_map                                          run_config_beam.py
--------------------------------------------------------------------------------
 What : iface -> which hybrid/VMM ids appear in the data (labels QA plots).
 How  : take one short run (or the dry run below) and open the QA
        hits_per_vmm plot / events.json "hits_per_vmm" keys — the VMM ids
        present are the map. Match them to the physical hybrids from the
        cabling sheet.

--------------------------------------------------------------------------------
 10. Backup to EOS                                   backup_config.py
--------------------------------------------------------------------------------
 What : EOS_DIR, CERN_PRINCIPAL, GPG_PASS_FILE.
 How  : EOS_DIR — the experiment's EOS space, e.g. an existing
            /eos/project/.../p2_sps_beam/ or your user space
            /eos/user/<initial>/<username>/p2_sps_beam/  (create it via
            lxplus or CERNBox first). It must be FUSE-mounted on the DAQ
            machine (ls /eos/... works).
        CERN_PRINCIPAL — your CERN account, e.g. akallits@CERN.CH
            (check with:  klist  after a kinit on lxplus).
        GPG_PASS_FILE — store your CERN password encrypted:
            echo -n '<password>' | gpg --encrypt -r <your-gpg-key> -o ~/.cern_pass.gpg
        Test the chain once:
            gpg -d ~/.cern_pass.gpg | kinit <principal>
            rsync -a --dry-run <base_data_dir>/config/ /eos/<...>/config/

--------------------------------------------------------------------------------
 11. Credentials / privileges on the DAQ machine
--------------------------------------------------------------------------------
 - hv_creds.txt in the repo root: CAEN username on line 1, password on
   line 2 (gitignored). Ask whoever administers the crate.
 - dumpcap without sudo:
       sudo usermod -aG wireshark $USER    # then re-login, or:
       sudo setcap cap_net_raw,cap_net_admin+eip $(which dumpcap)
   Verify:  dumpcap -D    lists the interfaces without sudo.
 - alinx-sc on PATH for the shell that runs start_servers.sh:  which alinx-sc

--------------------------------------------------------------------------------
 12. Beam monitor (SPS Page 1 -> ON/OFF chip, history, alerts)
--------------------------------------------------------------------------------
 Nothing to configure in code — it works out of the box once Flask is up —
 but three things to check/set on site:

 a) Reachability: the DAQ machine must reach the Vistar image host:
        curl -sI https://vistar-capture.s3.cern.ch/sps1.png   # expect 200
    (It is a CERN host, so it works from inside CERN even on restricted
    networks; if it fails, check the machine's proxy/egress rules.)
    If HTTPS transfers stall but plain HTTP works (some site firewalls
    throttle sustained TLS — the Saclay bench does), set in
    config/beam_config.json:  "vistar_scheme": "http"
    and optionally slow the poll:  "poll_s": 60

 b) Target + threshold (Beam Monitor strip in the GUI): pick the SPS target
    feeding OUR beamline — T2 -> H2/H4, T4 -> H6/H8, T6 -> M2/AMBER,
    T10 -> K12/NA62 (default is T2; check which hall P2 is in). Threshold
    default 1.0 E11 is far below normal intensities (~30-50) and above
    parasitic noise (~0.1-0.2 seen with beam off) — only touch it if the
    chip flickers. Both are persisted in config/beam_config.json.
    Sanity check: "Show page" — the chip state should match what the SPS
    page shows for that target.

 c) Off-period log: logs/beam_history.csv (gitignored) records every
    ON/OFF transition with the off-duration; also served at /beam_history
    (linked from the strip). Delete the file once before production if you
    want it to start clean — it may contain test transitions.

 Optional — beam-off alerts to your phone: Monitoring -> Setup in the GUI.
    Telegram: bot token from @BotFather into config/monitor_config.json,
        message the bot once, click Auto-fetch, then Test Alert.
    WhatsApp: free CallMeBot gateway — save +34 644 51 95 23 as a contact,
        WhatsApp it "I allow callmebot to send me messages", enter your
        number + the API key it returns in the Setup panel. (Third-party
        relay; fine for status pings, delivery can lag.)
    Then toggle Monitoring ON. rule_beam_off alerts after 5 min of no beam
    and repeats hourly while it stays off (rule_options in
    config/monitor_config.json to tune) — it also covers dead DAQ sessions,
    HV/LV trouble and capture errors.

--------------------------------------------------------------------------------
 13. Final verification (in order)
--------------------------------------------------------------------------------
 [ ] grep -rn TODO-SPS *.py        -> only comments/doc lines remain
 [ ] SITE = 'sps'; .venv/bin/python run_config_beam.py  -> summary correct
 [ ] ping + service tests of items 3/5/6 all pass
 [ ] ./start_servers.sh ; each pane healthy (see README "First-connection tests")
 [ ] Beam Monitor chip shows ON/OFF matching the SPS page for our target
     (item 12); if alerts wanted, Test Alert arrives on the phone
 [ ] Dry run WITHOUT beam: N_SUBRUNS=1, SUBRUN_MIN=1-2 -> HV ramps, LV CSV
     fills, pcapngs rotate in runs/run_N/.../raw_daq_data/, QA PNGs appear,
     "Hits this run" counts, Stop Run shuts down cleanly
 [ ] Set the real run plan (N_SUBRUNS/SUBRUN_MIN/HV points) and go.

================================================================================
 Ports used (must not clash with the Dream stack on the same machine):
   hv_control 2100 | vmm_daq 2101 | lv_control 2102 | flask GUI 5002
 The Dream stack keeps its own ports (incl. flask 5001) — both can run at once.
================================================================================
